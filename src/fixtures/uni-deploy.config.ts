// eslint-disable-next-line n/no-unpublished-import
import { defineConfig } from '../config';

export default defineConfig({
  from: 'uni-deploy.config.ts',
  hello: {
    config: 'uni-deploy.config.ts',
  },
});
